# Eureka Server 포트
server:
  port: 8761

# 서비스 이름
spring:
  application:
    name: discovery-service

# Eureka 설정
eureka:
  client:
    register-with-eureka: false # 자기 자신을 등록하지 않음
    fetch-registry: false # 다른 Eureka에서 레지스트리 가져오지 않음
  instance:
    prefer-ip-address: true # IP 주소 우선 사용
